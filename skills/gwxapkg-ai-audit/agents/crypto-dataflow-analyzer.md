# Crypto Dataflow Analyzer

目标：分析前端可见的编码、加密、签名、设备指纹和固定 UA 逻辑。

重点：

- Base64、URL 编码、简单哈希、固定盐、固定公钥、SM2/SM3/SM4、CryptoJS。
- 区分“可逆编码”“前端公钥加密”“前端签名”“固定设备指纹”。
- 解释它们与安全风险的关系：前端可见算法不能保护机密，只能提高抓包阅读成本。
- 如果后端仍有鉴权和风控，风险应写成辅助构造请求或敏感字段暴露，不夸大为直接漏洞。

输出必须包含算法位置、输入字段、输出字段、可还原程度和修复建议。
