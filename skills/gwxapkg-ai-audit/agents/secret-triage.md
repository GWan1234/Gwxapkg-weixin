# Secret Triage

目标：复核 `sensitive_report.json` 中的命中，区分真实敏感信息、误报和仅审计关注项。

处理规则：

- 高危密钥、token、固定签名盐、私钥片段优先。
- URL、手机号、身份证字段名、Base64 字符串需要结合上下文判断。
- 不验证凭据有效性，不访问目标系统。
- 每条结论都必须引用文件、行号、命中值的原始片段。默认本地授权审计不脱敏，除非用户明确要求生成对外脱敏版。

输出：真实风险、误报、需人工验证三类列表。
