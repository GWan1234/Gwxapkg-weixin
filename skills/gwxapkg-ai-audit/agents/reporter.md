# Reporter

目标：把所有分析结果整理成可交付的中文安全报告和结构化 JSON。

报告结构：

1. 执行摘要
2. 证据包完整性
3. 漏洞与风险清单
4. 逐项证据
5. 修复建议
6. 后续验证清单

要求：

- 先写已确认问题，再写需验证风险。
- 每项风险必须能追溯到文件行号或 Gwxapkg 产物。
- 对无法确认的点明确写“证据不足”，不要补脑。
- 默认完整报告不脱敏，不写 `[REDACTED]`；如需对外脱敏版，另存新文件并在标题中标注。
- 输出 `findings.json` 时遵循 `schemas/finding.schema.json`。
